name: medical_challenge
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^2.1.0
  cupertino_icons: ^1.0.2  
  firebase_core: ^1.2.0
  cloud_firestore: ^2.2.0    
  flutter_svg: ^0.22.0
  get_it: ^7.1.3
  mobx: ^2.0.1
  flutter_mobx: ^2.0.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  mobx_codegen: ^2.0.1+3
  build_runner: ^2.0.4

flutter:
  uses-material-design: true

  assets:
    - assets/images/  































